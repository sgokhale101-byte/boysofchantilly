# Netlify config for the Boys of Chantilly dashboard.
# No build step: the site is static HTML plus one serverless function.
[build]
  publish = "public"

[functions]
  directory = "netlify/functions"
